<!DOCTYPE html>
<html lang=ja>

<head>
    <meta charset="utf-8">
    <title>sample</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>

    <h1>家計簿アプリ</h1>
    <div>
        <button onclick="location.href='/tags'">カテゴリ追加ページへ</button>
    </div>
    <div class="countainer mt-3 mb-3 text-center">
        <form method="post" action="/expenses">
            <div class="title">
                <label for="name">タイトル</label>
                <input type="text" name="title" required>
            </div>
            <div class="title">
                <label for="name">金額</label>
                <input type="number" name="amount" required>
            </div>
            <div class="title">
                <label for="name">分類タグ</label>
                <input type="number" name="TagId" required>
            </div>
            <div class="title">
                <label for="name">日付</label>
                <input type="text" name="date" required>
            </div>
            <div class="mt-3 text-center">
                <input class="btn btn-outline-primary" type="submit" value="登録">
            </div>
        </form>
    </div>
    <div class="mt-3 container">
		<div class="text-center">
			<form method="get" action="/expenses">
				<input type="number" name="tagId">
				<input class="btn btn-outline-primary" type="submit" value="カテゴリを指定">
			</form>
		</div>
        {% for expense in expenses %}
        <div class="border border-radius">
            <p>{{ expense.id }} : {{ expense.title }} {{expense.amount}} {{expense.date}}</p>
        </div>
        {% endfor %}
    </div>
    <h1>
        {{ sum }}
    </h1>
</body>

</html>